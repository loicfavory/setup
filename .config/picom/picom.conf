backend = "glx";

vsync = true;

fading = true;
fade-delta = 2;
fade-exclude = [
  "class_g != 'Rofi'"
];

shadow = true;
shadow-radius = 18;
shadow-opacity = 0.2;
shadow-exclude = [
  "class_g != 'Rofi'"
];

active-opacity = 0.99
inactive-opacity = 0.94


wintypes:
{
  normal = { full-shadow = true; };
};