
type = internal/fs
interval = 25

mount-0 = /
mount-1 = /home
label-mounted = %{F#88c0d0}%mountpoint%%{F-} %percentage_free%%

label-unmounted = %mountpoint% not mounted
label-unmounted-foreground = ${colors.disabled}